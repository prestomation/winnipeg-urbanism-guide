name: Deploy

on:
  push:
    branches: ["main"]
  workflow_dispatch:

jobs:
  deploy:
    permissions:
      contents: write
    uses: prestomation/urbanism-guide-core/.github/workflows/deploy.yml@v0.3.0
    with:
      timezone: "America/Winnipeg"
